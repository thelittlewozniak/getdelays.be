@using GetDelaysAPI;
@{
    ViewBag.Title = "SearchStation";
    List<Station> stat = ViewBag.s;
}

<h2>Search a Station</h2>
<ul>
    <li id="search">
        <input type="text" id="stat" onkeydown="ListStation()" />
    </li>
    @foreach (Station s in stat)
    {
        <li class="station" style="display:none">@Html.ActionLink(s.stationinfo.name, "GetStationsByName", "Station", routeValues: new { station = s.stationinfo.name }, htmlAttributes: null)</li>
    }
</ul>
